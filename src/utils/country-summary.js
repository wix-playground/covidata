export const CountrySummary = (name, slug, countryCode, totalConfirmed, newConfirmed, newDeaths, totalDeaths, newRecovered, totalRecovered) => {
  return (
    {
      name,
      slug,
      countryCode,
      totalConfirmed,
      newConfirmed,
      newDeaths,
      totalDeaths,
      newRecovered,
      totalRecovered
    })
}
